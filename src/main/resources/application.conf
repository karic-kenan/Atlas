ktor {
    deployment {
        environment = development
        autoreload = true
        port = 7003
        port = ${?PORT}
        rootPath = /api/v1/
    }
    application {
        modules = [ aethibo.io.ApplicationKt.mainModule ]
    }
}
